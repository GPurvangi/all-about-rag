# Database
DATABASE_URL=postgresql+asyncpg://rag:rag@localhost:5432/all_about_rag

# Redis (ARQ job queue)
REDIS_URL=redis://localhost:6379/0

# Provider selection (openai | deepgram | local — add adapters as needed)
TRANSCRIPTION_PROVIDER=openai
EMBEDDING_PROVIDER=openai
LLM_PROVIDER=openai

# API keys
OPENAI_API_KEY=sk-...

# Local audio blob storage
AUDIO_STORAGE_PATH=./data/audio

# App
API_HOST=0.0.0.0
API_PORT=8000
CORS_ORIGINS=http://localhost:3000
